import React from 'react';
import { View, Text } from 'react-native';

function SignUp() {
    return (
        <View>
            <Text>Sign Up</Text>
        </View>
    );
}

export default React.memo(SignUp);
